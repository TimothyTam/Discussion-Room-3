procedure p1{ a = b;
	while x {
		y = z;
		if acd then {
			asdf = x * (y-3) *(x+3)-((x*y));
			asf = a * 3 + b - c * 3;
			while m {
				easy = hard;}
			mn = asd;}
		else { abc = cd;
			asf = asdf; }
		asf = sdf;}
	while m {
		d= x;
		x = y;}
	wfefe =fasd;}